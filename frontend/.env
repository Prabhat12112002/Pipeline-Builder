# Frontend dev-server + API configuration (local development).
# See .env.example for documentation.

# Backend API base URL (configurable — used by submit.js)
REACT_APP_API_URL=http://localhost:8000

# Dev server settings
BROWSER=none
PORT=3000
DISABLE_ESLINT_PLUGIN=true
